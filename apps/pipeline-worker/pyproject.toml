[project]
name = "pipeline-worker"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "pika>=1.4.2",
    "httpx>=0.28.1",
    "google-genai>=1.15.0",
    "boto3>=1.35.0",
    "pyyaml>=6.0",
]

[project.scripts]
pipeline-worker = "pipeline_worker:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
